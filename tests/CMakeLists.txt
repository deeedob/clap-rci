if (clap-rci_BUILD_AUTO_TESTS)
    find_package(Catch2 3 REQUIRED)
    include(CTest)
    include(Catch)
    add_subdirectory(auto/)
endif ()

if (clap-rci_BUILD_MAN_TESTS)
    include(../cmake/clap_autolink.cmake)
    add_subdirectory(man/)
endif ()
